
/**
 * Defines shared constants used throughout the application to ensure consistency
 * and improve maintainability.
 */

// The default width of a node component in pixels, used for text nodes
// and as a fallback for image nodes.
export const DEFAULT_NODE_WIDTH = 320;

// The default height of a node component in pixels, used for text nodes
// and as a fallback for image nodes.
export const DEFAULT_NODE_HEIGHT = 320;

// The maximum width a dynamically-sized node can have.
export const MAX_NODE_WIDTH = 300;

// The maximum height a dynamically-sized node can have.
export const MAX_NODE_HEIGHT = 450;
